import { Formulario, Container, HeroTitle } from './Hero'

const Hero = () => (
  <Formulario>
    <Container>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Container>
  </Formulario>
)

export default Hero
